## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Icons for the sidebar and UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts

## Notes
Static site with hardcoded mock data for a documentation-style "Survival Guide".
No API endpoints required as per the prompt.
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
